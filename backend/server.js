import app from "./app.js"

const PORT = 3000
//sadadasdassadadasdas
app.listen(PORT, () => {
  console.log(`Server running at: http://localhost:${PORT}`)
  console.log(`Student API: http://localhost:${PORT}/api/`)
})